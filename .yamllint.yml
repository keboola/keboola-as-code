---
# .yamllint.yml
extends: default
ignore: |
  /vendor/
  /provisioning/
  /target/
  **/openapi*.yaml
rules:
  line-length:
    max: 240  # For long commands in Taskfile
  indentation:
    spaces: consistent
    indent-sequences: true
  truthy:
    allowed-values: ['true', 'false', 'on']
