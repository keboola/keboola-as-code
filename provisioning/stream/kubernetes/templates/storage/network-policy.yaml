---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: stream-storage
  namespace: $NAMESPACE
  labels:
    app: stream-storage
    releaseId: $RELEASE_ID
spec:
  podSelector:
    matchLabels:
      app: stream-storage
  policyTypes:
    - Ingress
  ingress:
    # Metrics
    - ports:
        - port: 9000
      from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: datadog
          podSelector:
            matchLabels:
              app: datadog-agent
