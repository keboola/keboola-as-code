Command "local fix-paths"

Manifest file ".keboola/manifest.json" contains a naming for all local paths.

With this command you can rename all existing paths
to match the configured naming (eg. if the naming has been changed).

Use "--cleanup" to remove conflicting destination paths and retry renames when necessary.

Usage:
  %s fix-paths [flags]

Flags:
      --cleanup                    remove conflicting destination paths during rename
      --dry-run                    print what needs to be done
  -H, --storage-api-host string    storage API host, eg. "connection.keboola.com"
  -t, --storage-api-token string   storage API token from your project

Global Flags:
%A
