Command "remote create"

Create an object in the project.

Usage:
  %s remote create [command]

Available Commands:
  remote create branch  Create a new branch from the main branch.
  remote create bucket  Create a new bucket.
  remote create table   Create a new table.

Flags:
  -H, --storage-api-host string    storage API host, eg. "connection.keboola.com"
  -t, --storage-api-token string   storage API token from your project

Global Flags:
%A
