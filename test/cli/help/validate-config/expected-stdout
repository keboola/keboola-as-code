Command "local validate config"

Each component definition optionally contains
a schema of the configuration parameters key.

The command validates the content of the specified
JSON file against the schema. It can be used both
in a project local directory and also separately.

Usage:
  %s local validate config component.id config.json [flags]

Flags:
  -H, --storage-api-host string    storage API host, eg. "connection.keboola.com"
  -t, --storage-api-token string   storage API token from your project

Global Flags:
%A
