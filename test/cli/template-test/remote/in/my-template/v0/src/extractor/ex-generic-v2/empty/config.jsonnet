{
  "parameters": {
    "api": {
      "baseUrl": "https://connection.keboola.com"
    },
    "config": {
      "jobs": [
        {
          "dataField": "api",
          "endpoint": "v2/storage?exclude=components"
        }
      ],
      "outputBucket": "test"
    }
  }
}
