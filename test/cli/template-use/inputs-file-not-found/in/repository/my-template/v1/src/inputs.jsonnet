{
  inputs: [
    {
      id: "base-url",
      name: "Base URL",
      description: "Base URL for Generic Extractor",
      kind: "input",
      type: "string",
      rules: "required,url",
    },
  ],
}
